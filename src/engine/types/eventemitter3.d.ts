// eventemitter3 has its own types - this file is intentionally minimal
// to avoid conflicting with the package's built-in type declarations.
// The TypedEmitter wrapper in src/utils/events.ts provides our typed interface.
